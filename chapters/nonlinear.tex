\chapter{Non-Linear Supervised Alignment Methods}
\label{ch:nonlinear}

While the \gls{cca} + Affine baseline of Chapter~\ref{ch:baseline} establishes a
strong supervised linear upper bound, it is inherently constrained to learning
affine transformations between the two modalities. This chapter investigates
two non-linear extensions that operate within the same supervised paradigm
--- leveraging the same 30,000 ground-truth anchor pairs --- but exploit
more expressive, non-linear geometric structures to potentially close the
remaining performance gap.

Both methods are evaluated under identical experimental conditions to those
of Chapter~\ref{ch:baseline}: $n_A = 30{,}000$ anchor pairs, $k = 50$ \gls{cca}
subspace, 500 held-out query pairs, averaged over three independent random seeds.

%% ─────────────────────────────────────────────────────────────────────────────
\section{Kernel CCA with Nyström Approximation}
\label{sec:kcca}

\subsection{Motivation}
\label{subsec:kcca_motivation}

Standard \gls{cca} discovers only linear correlations between two spaces. However,
the relationship between 3D geometric representations and natural language
embeddings may contain non-linear structure that a linear projection cannot
capture. Kernel CCA (KCCA) extends \gls{cca} by implicitly mapping features into
a higher-dimensional Reproducing Kernel Hilbert Space (RKHS) via a kernel
function, allowing the discovery of non-linear correlations without explicitly
computing the high-dimensional feature map.

\subsection{Implementation}
\label{subsec:kcca_implementation}

\gls{kcca} replaces the dot product in the standard covariance computation with
a kernel evaluation:
\begin{equation}
    K(x, x') = \exp\!\left(-\gamma \|x - x'\|^2\right)
    \label{eq:rbf_kernel}
\end{equation}
where $\gamma = 0.1$ is the \gls{rbf} bandwidth parameter. The resulting kernel
matrix $K \in \mathbb{R}^{n \times n}$ captures non-linear similarity
between samples in the original feature space.

Computing \gls{kcca} exactly requires inverting an $n \times n$ matrix, which
scales as $\mathcal{O}(n^3)$ --- computationally infeasible at $n = 30{,}000$
anchors. To address this, we employ the \textbf{Nyström approximation}
\cite{williams2001nystrom}, which approximates the full kernel matrix using
a randomly selected subset of $m = 500$ landmark points:
\begin{equation}
    K \approx K_{nm}\, K_{mm}^{-1}\, K_{mn}
    \label{eq:nystrom}
\end{equation}
where $K_{nm} \in \mathbb{R}^{n \times m}$ contains kernel evaluations between
all $n$ training points and the $m$ landmarks, and $K_{mm} \in \mathbb{R}^{m
\times m}$ is the kernel matrix restricted to the landmarks. This reduces the
computational cost to $\mathcal{O}(m^2 n)$, making \gls{kcca} tractable at full
dataset scale.

\subsection{Results}
\label{subsec:kcca_results}
\begin{table}[h]
\centering
\resizebox{\textwidth}{!}{%
\begin{tabular}{lcccccc}
\toprule
\textbf{Pair} & \textbf{CA Match} & \textbf{CA Top-5}
    & \textbf{KCCA Match} & \textbf{KCCA Top-5}
    & \textbf{RML Match} & \textbf{RML Top-5} \\
\midrule
PointNet $\times$ CLIP      & 13.7\% & 34.1\% & 4.7\% & 15.8\% & 9.4\%  & \textbf{27.2\%} \\
PointNet $\times$ RoBERTa   &  9.1\% & 27.8\% & 3.1\% & 13.1\% & 4.6\%  & 17.3\% \\
PointNet $\times$ BERT      &  6.9\% & 25.4\% & 3.1\% & 12.4\% & 5.1\%  & 16.5\% \\
SparseConv $\times$ CLIP    &  3.5\% & 13.2\% & 1.2\% &  6.3\% & 3.7\%  & 10.7\% \\
SparseConv $\times$ RoBERTa &  3.5\% & 13.9\% & 1.5\% &  6.1\% & 2.5\%  &  9.7\% \\
SparseConv $\times$ BERT    &  2.0\% & 10.2\% & 1.4\% &  5.4\% & 2.7\%  &  8.3\% \\
\bottomrule
\end{tabular}%
}
\caption{Non-linear methods compared to CCA+Affine (CA) across all encoder
pairs. KCCA: Kernel CCA with Nyström approximation ($\gamma$=0.1, $m$=500
landmarks). RML: Riemannian Metric Learning (batch=256, lr=0.01, 200
iterations). RML reaches 27.2\% Top-5 on PointNet$\times$CLIP, the closest
alternative to CCA+Affine at 76\% of its performance.
CA results reflect the specific CCA+Affine run executed alongside this
experiment; see the note on run-to-run variance in
Section~\ref{sec:baseline_results} (Chapter~\ref{ch:baseline}) for the
representative $35.7\%$ value used elsewhere in this thesis. Protocol:
30,000 anchor pairs, 500 query points, $k=50$, averaged over 3 random
seeds.}
\label{tab:nonlinear_results}
\end{table}
Kernel \gls{cca} with Nyström approximation achieves 15.8\% Top-5 on
PointNet$\times$\gls{clip}, an improvement over all unsupervised \gls{gw} methods but
considerably below the linear \gls{cca}+Affine baseline. The gap suggests that
the non-linear structure captured by the \gls{rbf} kernel does not provide
substantially more


\begin{table}[htpb]
\centering
\renewcommand{\arraystretch}{1.2}
\resizebox{\textwidth}{!}{%
\begin{tabular}{llcccc}
\toprule
\textbf{Category} & \textbf{Method}
    & \textbf{Match} & \textbf{Top-1} & \textbf{Top-5} & \textbf{Top-10} \\
\midrule
\multicolumn{6}{l}{\textit{Unsupervised Optimal Transport}} \\
& Sliced GW ($k$=20, 5k anc.)        & 1.5\% & 1.1\% & 3.2\%           & 4.8\%  \\
& FGW ($\alpha$=0.2, 1k anc.)        & 3.6\% & 3.6\% & 4.4\%           & 5.4\%  \\
\midrule
\multicolumn{6}{l}{\textit{Supervised Linear}} \\
& CCA + Affine (30k anc.)            & 13.5\% & 14.6\% & \textbf{35.7\%} & 46.4\% \\
\midrule
\multicolumn{6}{l}{\textit{Supervised Non-Linear (this chapter)}} \\
& KCCA Nystr\"{o}m ($\gamma$=0.1)    & 4.7\%  & 4.9\%  & 15.8\%          & 23.3\% \\
& Riemannian Metric Learning         & 9.4\%  & 8.7\%  & 27.2\%          & 38.4\% \\
\midrule
\multicolumn{6}{l}{\textit{Random Baseline}} \\
& Independent Gaussian               & $\approx$0.6\% & $\approx$0.2\%
                                     & $\approx$1.6\% & $\approx$2.0\% \\
\bottomrule
\end{tabular}%
}
\caption{Complete ranking of all tested alignment methods on PointNet
$\times$ CLIP. Final Top-5 order: CCA+Affine (35.7\%) $>$ RML (27.2\%)
$>$ KCCA (15.8\%) $>$ FGW (4.4\%) $>$ SGW (3.2\%) $\approx$ random
($\approx$1.6\%). CCA+Affine result taken from
Table~\ref{tab:linear_baseline_results} (Chapter~\ref{ch:baseline}).
All results: 30,000 anchor pairs, 500 query points, $k=50$,
averaged over 3 seeds.}
\label{tab:complete_ranking}
\end{table}